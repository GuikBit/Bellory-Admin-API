package org.exemplo.belloryadmin.model.entity.lead.enums;

/**
 * Tipo de negocio informado no formulario de contato.
 * Espelha o enum do schema do site (CONTACT_BACKEND_SPEC.md).
 */
public enum TipoNegocio {
    SALAO,
    BARBEARIA,
    CLINICA,
    NAIL,
    SPA,
    OUTRO
}
